<h2><%= link_to "Back to Post Archive", posts_path, class: "back" %></h2>

	<h1><%= @post.title %></h1><br>
	<h2><%= @post.created_at.in_time_zone("Eastern Time (US & Canada)").strftime("%b %d, %Y | %I:%M%p") %></h2>
	<p><%= @post.text %></p>
